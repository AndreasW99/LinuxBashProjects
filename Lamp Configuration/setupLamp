1) Install Apache using command:
      root@ sudo apt-get install apache2 -y
      
      enable and start apache
      root@ systemctl start apache2
      root@ systemctl enable apache2
      
localhost will display apache2 Ubuntu default page
      
2) Install mariaDB database
      root@ sudo apt-get install mariadb-server mariadb-client -y
      
      enable and start mariadb
      root@ systemctl start mariadb
      root@ systemctl enable mariadb

3) Execute mysql installation
      root@ mysql_secure_installation
      
      root@ mysql -u root -p
 
4) Install PHP
      root@ sudo apt-get install php php-mysql-y
      
      root@ nano /var/www/html/phpinfo.php
      **testing the php installation to print php info page
      
<?php
phpinfo()
?>

roo@ systemctl retsart apache2
